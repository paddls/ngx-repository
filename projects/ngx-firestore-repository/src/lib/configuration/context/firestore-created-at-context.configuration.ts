import {PropertyKeyConfiguration} from '@witty-services/ngx-repository';

export interface FirestoreCreatedAtContext {

  field: string;
}

/**
 * @ignore
 */
export interface FirestoreCreatedAtContextConfiguration extends FirestoreCreatedAtContext, PropertyKeyConfiguration {
}
